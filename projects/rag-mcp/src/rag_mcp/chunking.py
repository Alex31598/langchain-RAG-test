"""Hierarchical / parent-child chunking with id linkage.

Skeleton stub. Small "child" chunks (~300-500 tokens) for retrieval mapped to
larger "parent" chunks (~1200-2000 tokens) returned to the LLM, with parent-id
metadata linkage stored in Qdrant, are implemented in M3.3.
"""
