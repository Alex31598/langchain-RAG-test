"""Ollama local embeddings (bge-m3 primary, nomic-embed-text fallback).

Skeleton stub. The Ollama embedding client and fallback logic are implemented
in M4.1. Ollama is reached over the internal mcpnet network only (no host port).
"""
