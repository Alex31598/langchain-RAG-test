"""FastMCP server entrypoint (HTTP/SSE) exposing RAG tools.

Skeleton stub. The tools (``search_documents``, ``get_chunk``,
``get_document_metadata``, ``list_collections``, ``answer_with_context``),
bearer auth, input bounding, ``<context>`` fencing, and output redaction are
implemented in M4.7. Run with ``python -m rag_mcp.server`` (container CMD).
"""
