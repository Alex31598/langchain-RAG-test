"""FastMCP server entrypoint (HTTP/SSE) for the telemetry MCP.

Skeleton stub. The real tools, bearer auth, and input bounding are implemented
in M1.5. Run with ``python -m telemetry_mcp.server`` (container CMD).
"""
